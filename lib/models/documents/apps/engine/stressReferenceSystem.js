const StressReferenceSystem = {
  min: 1,
  global: 1,      // The global Cartesian coordinate system (X, Y, Z).
  material: 2,    // The element material system for orthotropic materials.
  max: 2,
};

module.exports = StressReferenceSystem;